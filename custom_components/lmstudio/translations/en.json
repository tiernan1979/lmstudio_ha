{
  "config": {
    "step": {
      "user": {
        "title": "Connect to LM Studio",
        "description": "Enter the URL of your running LM Studio instance.",
        "data": {
          "url": "LM Studio URL",
          "api_key": "API Key (optional)"
        }
      },
      "options": {
        "title": "Configure LM Studio",
        "description": "Select models and configure behavior.",
        "data": {
          "model": "Default Model",
          "smart_model": "Smart Model (for complex queries, optional)",
          "fast_model": "Fast Model (for short queries, optional)",
          "system_prompt": "System Prompt",
          "idle_timeout": "Idle Timeout (minutes)",
          "streaming": "Enable Streaming",
          "thinking": "Thinking Mode"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to LM Studio. Check the URL and ensure LM Studio is running.",
      "no_models": "Connected but no models found. Load a model in LM Studio first."
    },
    "abort": {
      "already_configured": "LM Studio is already configured."
    }
  },
  "options": {
    "step": {
      "options": {
        "title": "LM Studio Options",
        "description": "Update your LM Studio configuration.",
        "data": {
          "model": "Default Model",
          "smart_model": "Smart Model (for complex queries, optional)",
          "fast_model": "Fast Model (for short queries, optional)",
          "system_prompt": "System Prompt",
          "idle_timeout": "Idle Timeout (minutes)",
          "streaming": "Enable Streaming",
          "thinking": "Thinking Mode"
        }
      }
    }
  },
  "entity": {
    "select": {
      "model": {
        "name": "Model"
      }
    }
  }
}
